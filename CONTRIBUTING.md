# Contributing

Add each extension under `plugins/<name>` with its own README, source packages, integration instructions, and tests. Keep Host and Client concerns separate, use lifecycle-bound Cordis effects, and document every persistence or permission boundary.
